//interface for post events
export interface PostEventProps {
  name: string;
  title: string;
  date: string;
  time: string;
  location: string;
  description: string;
  image?: string;
}

export interface PostTestimonialProps {
  name: string;
  about: string;
  achievement: string;
  image?: string;
}
